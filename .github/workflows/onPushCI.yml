name: On Push CI
on:
  push:
    branches:
      - '*CI'
      - '!master'

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Run unit and integration tests
        run: |
          npm run ci:test
